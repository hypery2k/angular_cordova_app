angular_cordova_app - server
============================

Simple JAX-RS web application do you how to handle REST interfaces with AngularJS

Use the ``ùsers.sql``` to add initial users

Deploy to local JBoss

mvn -Pdeploy-local-jboss install